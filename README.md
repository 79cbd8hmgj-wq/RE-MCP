# RE-MCP

A safety-first Model Context Protocol server for ROM reverse-engineering workflows.

The first release targets local stdio use and wraps allowlisted repository, test, ROM-build, emulator, and debugger operations without exposing an unrestricted shell.
