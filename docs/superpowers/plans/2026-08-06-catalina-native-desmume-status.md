# Native Build Status

Code preparation is complete. Native compilation has not yet run because this environment cannot start workflow_dispatch jobs. The next action is the manual GitHub Actions run documented in the repository README.
